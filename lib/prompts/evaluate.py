"""R3 深度评估 prompt 模板"""

EVALUATE_PROMPT = """你是代码审查验证专家。请对以下 {module_name}（{language}）模块的扫描发现进行深度评估。

**你的任务不是判断 bug 是否"存在"，而是判断它是否"值得修"。**

## 评估方法（按此顺序执行）

对每个 finding，必须**先读代码再判定**，不能只看 finding 描述：

### 第一步：读代码复现路径
- 打开 finding 指向的文件和行号，读完整函数
- 沿错误路径/异常路径追踪：catch 块做了什么？finally 有没有？调用者怎么处理失败？
- **追踪 2-3 层调用链**：不要只看当前函数，往上看调用者、往下看被调用者

### 第二步：检查现有保护
- 在**同一代码库**中搜索类似场景的处理模式。如果其他地方有保护（guard/check/retry），而此处没有，这是"遗漏"而非"设计如此"
- 检查上层调用者是否有 try/catch、重试、状态重置机制能兜底

### 第三步：追踪衍生影响链（最重要）
- 假设这个 bug 真的触发了，系统状态会变成什么样？
- 这个"错误状态"会不会阻塞后续操作？比如：标志位未重置 → 后续功能被 guard 拦截 → 用户必须重启
- **衍生影响可能比原始描述严重得多**——资源泄漏可能导致功能失效，状态不一致可能导致数据丢失

### 第四步：评估触发概率
- 描述触发所需的**具体用户操作序列**，不要只说"低概率"
- 考虑用户行为习惯：错误后用户通常会做什么？等待还是重试？切应用还是留在原地？

{cross_module_section}

## 判定标准

- **must_fix**：真实可触发 + 用户可感知的功能影响（功能失效/数据丢失/粘贴到错误应用）+ 无自愈机制
- **verify**：影响不确定，需要红绿测试才能判断（这是默认选项——不确定时选这个）
- **skip**：以下任一条件满足即可 skip：
  - 触发需要极端条件（硬件故障/特定代理配置/毫秒级竞态窗口）
  - 影响仅为日志噪音或短暂卡顿（<1 秒）
  - 同一调用链上有完善的上层保护能兜底
  - 纯代码风格/防御性编程（defer 顺序不优雅但不影响行为）

**重要**：skip 不是"这个 bug 不存在"，而是"存在但不值得花 50K token 去红绿验证"。

## 待评估的发现

{findings_json}

请逐一读取代码，按上述四步执行后给出评估。"""

# 跨模块上下文段落（有边界信息时注入）
CROSS_MODULE_SECTION = """### 跨模块验证（关键）
以下是与 {module_name} 有通信关系的对端模块信息：
{cross_module_info}

**必须检查**：
- 对端模块是否有保护机制能阻止本模块的 bug 被触发？（如：服务端状态机是否阻止了客户端的非法转换？）
- 本模块的错误处理假设（如"服务端会发送 X"）在对端代码中是否成立？
- 如果对端有完善保护，本模块的 bug 可能永远不会被触发——这种情况应判定 skip"""

# 无跨模块上下文时的空段落
NO_CROSS_MODULE_SECTION = ""
